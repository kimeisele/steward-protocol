package:
  name: "agent-city-core"
  version: "1.0.0"
  description: "The Governance & Operations Core for VibeOS"
  license: "MIT"
  author: "Vibe Inc."

# Cartridges are now autonomously discovered by the Steward Agent
# from the `agent_city` directory.
# No hardcoded paths here.


dependencies:
  - "vibe-agency >= 2.0.0"
