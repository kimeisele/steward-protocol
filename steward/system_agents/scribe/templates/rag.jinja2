{#- RAG.md Template - Realtime Architecture Guide -#}
{#- 4D Multi-Dimensional Analysis Output -#}
{#- Variables: timestamp, context, kernel_status -#}
{#- context: { temporal, structural, semantic, predictive, executive_summary } -#}
{% include 'header.jinja2' %}
# RAG - Realtime Architecture Guide

> **Git is Truth.** This file provides real-time repository context through 4D analysis.
> Auto-generated by ANALYST agent (v2.0), rendered by SCRIBE.

---

## EXECUTIVE SUMMARY

{% if context.executive_summary %}
| Dimension | Status | Indicator |
|-----------|--------|-----------|
| **Velocity** | {{ context.executive_summary.velocity_trend }} | {{ context.executive_summary.momentum }} |
| **Stability** | {{ context.executive_summary.stability }} | {{ context.executive_summary.stability_indicator }} |
| **Tech Debt** | {{ context.executive_summary.debt_level }} | {% if context.executive_summary.debt_level == "LOW" %}🟢{% elif context.executive_summary.debt_level == "MODERATE" %}🟡{% elif context.executive_summary.debt_level == "HIGH" %}🟠{% else %}🔴{% endif %} |
| **Dominant Work** | {{ context.executive_summary.dominant_work | title }} | {% if context.executive_summary.dominant_work == "feature" %}🚀{% elif context.executive_summary.dominant_work == "fix" %}🔧{% elif context.executive_summary.dominant_work == "refactor" %}♻️{% elif context.executive_summary.dominant_work == "docs" %}📚{% elif context.executive_summary.dominant_work == "test" %}🧪{% else %}📦{% endif %} |

{% if context.executive_summary.suggestions %}
### 💡 Actionable Insights

{% for suggestion in context.executive_summary.suggestions %}
- {{ suggestion }}
{% endfor %}
{% endif %}

{% if context.executive_summary.priorities %}
### 🎯 Priorities

{% for priority in context.executive_summary.priorities %}
1. {{ priority }}
{% endfor %}
{% endif %}
{% endif %}

---

## 📅 TEMPORAL DIMENSION

*Time-based patterns and development rhythm*

{% if context.temporal %}
### Velocity Trend

| Metric | Value |
|--------|-------|
| **Trend** | {{ context.temporal.velocity_trend.trend }} {{ context.temporal.velocity_trend.momentum }} |
| **Avg Weekly Commits** | {{ context.temporal.velocity_trend.average_weekly }} |
| **Trend Ratio** | {{ context.temporal.velocity_trend.trend_ratio }} |

**Weekly Breakdown:**
{% for count in context.temporal.velocity_trend.weekly_counts %}
- Week {{ loop.index }}: {{ count }} commits
{% endfor %}

### Work Patterns

| Day | Commits | Activity |
|-----|---------|----------|
{%- for day, count in context.temporal.day_patterns.distribution.items() %}
| {{ day }} | {{ count }} | {{ '█' * (count // 2) if count else '░' }} |
{%- endfor %}

- **Peak Day:** {{ context.temporal.day_patterns.peak_day }} ({{ context.temporal.day_patterns.peak_count }} commits)
- **Work Style:** {{ context.temporal.hour_patterns.work_style }}
- **Peak Hour:** {{ context.temporal.hour_patterns.peak_hour_formatted }}
- **Weekend Ratio:** {{ (context.temporal.day_patterns.weekend_ratio * 100) | int }}%

### Activity Analysis

| Period | Metric | Value |
|--------|--------|-------|
| Morning (6-12) | Commits | {{ context.temporal.hour_patterns.morning }} |
| Afternoon (12-18) | Commits | {{ context.temporal.hour_patterns.afternoon }} |
| Evening (18-24) | Commits | {{ context.temporal.hour_patterns.evening }} |
| Night (0-6) | Commits | {{ context.temporal.hour_patterns.night }} |

{% if context.temporal.activity_bursts.burst_days %}
### 🔥 High Activity Days

{% for date, count in context.temporal.activity_bursts.burst_days %}
- `{{ date }}`: **{{ count }}** commits
{% endfor %}
{% endif %}
{% endif %}

---

## 🏗️ STRUCTURAL DIMENSION

*Architecture, dependencies, and coupling analysis*

{% if context.structural %}
### Co-Change Analysis

*Files that frequently change together reveal hidden coupling:*

| File Pair | Co-Changes |
|-----------|------------|
{%- for pair, count in context.structural.co_changes.co_change_pairs[:10] %}
| {{ pair }} | {{ count }} |
{%- endfor %}

### Coupling Hotspots

*Files with highest coupling scores:*

| File | Coupling Score |
|------|----------------|
{%- for file, score in context.structural.co_changes.coupling_hotspots[:8] %}
| `{{ file }}` | {{ score }} |
{%- endfor %}

### Architecture Hubs

*Most depended-upon modules (high incoming imports):*

| Module | Dependents |
|--------|------------|
{%- for module, count in context.structural.import_graph.architectural_hubs[:8] %}
| `{{ module }}` | {{ count }} |
{%- endfor %}

### Complex Files

*Files with highest outgoing dependencies:*

| File | Dependencies |
|------|--------------|
{%- for file, count in context.structural.import_graph.complex_files[:8] %}
| `{{ file }}` | {{ count }} |
{%- endfor %}

### Module Boundaries

| Module | Python Files | YAML Files | Total |
|--------|--------------|------------|-------|
{%- for module, stats in context.structural.module_boundaries.modules.items() %}
| `{{ module }}/` | {{ stats.python_files }} | {{ stats.yaml_files }} | {{ stats.total_files }} |
{%- endfor %}
{% endif %}

---

## 📝 SEMANTIC DIMENSION

*Work classification, complexity, and technical debt*

{% if context.semantic %}
### Commit Type Distribution

*What kind of work is happening:*

| Type | Count | Percentage | Bar |
|------|-------|------------|-----|
{%- for type, count in context.semantic.commit_types.distribution.items() | sort(attribute='1', reverse=true) %}
| {{ type | title }} | {{ count }} | {{ context.semantic.commit_types.percentages.get(type, 0) }}% | {{ '█' * ((context.semantic.commit_types.percentages.get(type, 0) / 5) | int) }} |
{%- endfor %}

**Dominant Work Type:** {{ context.semantic.commit_types.dominant_type | title }}

### Technical Debt Indicators

| Marker | Count | Locations |
|--------|-------|-----------|
{%- for marker, count in context.semantic.technical_debt.counts.items() %}
| {{ marker | upper }} | {{ count }} | {% for loc in context.semantic.technical_debt.hotspot_files.get(marker, [])[:2] %}`{{ loc }}`{% if not loop.last %}, {% endif %}{% endfor %} |
{%- endfor %}

**Debt Score:** {{ context.semantic.technical_debt.debt_score }}
**Debt Level:** {{ context.semantic.technical_debt.debt_level }} {% if context.semantic.technical_debt.debt_level == "LOW" %}🟢{% elif context.semantic.technical_debt.debt_level == "MODERATE" %}🟡{% elif context.semantic.technical_debt.debt_level == "HIGH" %}🟠{% else %}🔴{% endif %}

### Complexity Analysis

*Files with highest complexity scores:*

| File | Complexity |
|------|------------|
{%- for file, complexity in context.semantic.complexity.complex_files[:10] %}
| `{{ file }}` | {{ complexity }} |
{%- endfor %}
{% endif %}

---

## 🔮 PREDICTIVE DIMENSION

*Forward-looking insights and recommendations*

{% if context.predictive %}
### Stability Assessment

| Metric | Value |
|--------|-------|
| **Status** | {{ context.predictive.stability.stability }} {{ context.predictive.stability.indicator }} |
| **Fix Ratio** | {{ context.predictive.stability.fix_ratio }}% |
| **Revert Ratio** | {{ context.predictive.stability.revert_ratio }}% |
| **Period** | Last {{ context.predictive.stability.period_days }} days |

{% if context.predictive.risk_areas.high_risk_files %}
### ⚠️ Risk Areas

*Files with high churn + fix commits (likely unstable):*

| File | Risk Score | Fixes |
|------|------------|-------|
{%- for file, score, fixes in context.predictive.risk_areas.high_risk_files[:8] %}
| `{{ file }}` | {{ score }} | {{ fixes }} |
{%- endfor %}
{% endif %}

{% if context.predictive.relatedness_map %}
### 🔗 Related Files Map

*If working on file X, consider these related files:*

{% for file, related in context.predictive.relatedness_map.items() | list | batch(5) | first %}
**`{{ file }}`**
{%- for rel in related %}
  - `{{ rel }}`
{%- endfor %}

{% endfor %}
{% endif %}

{% if context.predictive.focus_suggestions.suggestions %}
### 💡 Focus Recommendations

{% for suggestion in context.predictive.focus_suggestions.suggestions %}
- {{ suggestion }}
{% endfor %}
{% endif %}
{% endif %}

---

## 📊 METADATA

| Field | Value |
|-------|-------|
| **Generated** | {{ timestamp }} |
| **Total Commits** | {{ context.total_commits }} |
| **Analysis Scope** | {{ context.metadata.scope if context.metadata else 'full' }} |
| **VEDA-4 Circuit** | {{ context.metadata.veda4_circuit if context.metadata else 'CONTEXT_SYNTH' }} |
| **Dimensions** | Temporal, Structural, Semantic, Predictive |

---

*RAG = Realtime Architecture Guide (NOT Retrieval Augmented Generation)*
*Powered by ANALYST v2.0 - 4D Multi-Dimensional Analysis Engine*
