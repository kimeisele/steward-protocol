{#- Unified Header for Auto-Generated Documents -#}
{#- Variables: timestamp, generator, kernel_status, quick_nav_links -#}
<!--
╔══════════════════════════════════════════════════════════════════════╗
║  AUTO-GENERATED by {{ generator }}
║  Last Updated: {{ timestamp }}
║  Kernel: {% if kernel_status.running %}🟢 {{ kernel_status.status }} ({{ kernel_status.agents }} agents){% else %}🔴 NOT RUNNING{% endif %}
{% if not kernel_status.running %}║  Quick Start: python boot.py                                         {% endif %}
╠══════════════════════════════════════════════════════════════════════╣
{% set max_width = 66 %}
{% set line = namespace(str='  📚 Auto-Docs: ') %}
{% for link in quick_nav_links %}
    {% set link_text = link.name %}
    {% if not loop.first %}{% set link_text = ' | ' ~ link_text %}{% endif %}
    {% if (line.str | length) + (link_text | length) > max_width %}
║{{ line.str.ljust(max_width) }}║
        {% set line.str = '                ' ~ link.name %}
    {% else %}
        {% set line.str = line.str ~ link_text %}
    {% endif %}
{% endfor %}
║{{ line.str.ljust(max_width) }}║
╚══════════════════════════════════════════════════════════════════════╝
-->