name: "🔗 Multi-Agent Federation (HERALD + ARCHIVIST)"

# Demonstrates STEWARD Protocol multi-agent coordination:
# 1. HERALD generates content (Agent #1)
# 2. ARCHIVIST audits HERALD's events (Agent #2)
# 3. Both agents operate independently but coordinate through events

on:
  # Manual trigger for testing
  workflow_dispatch:

  # Daily federation test
  schedule:
    - cron: '0 12 * * *'  # Daily at noon UTC

jobs:
  herald-generate:
    name: "🦅 HERALD - Content Generation"
    runs-on: ubuntu-latest

    steps:
      - name: "📥 Checkout Repository"
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: "🐍 Setup Python"
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: "📦 Install Dependencies"
        run: |
          pip install --upgrade pip
          pip install -r herald/requirements.txt

      - name: "🦅 HERALD - Generate Content (Dry Run)"
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
        run: |
          echo "🦅 Running HERALD in dry-run mode (no publication)..."
          python herald/shim.py --action run --dry-run

      - name: "📤 Upload HERALD Events"
        uses: actions/upload-artifact@v3
        with:
          name: herald-events
          path: |
            data/events/herald.jsonl
            data/history/*.jsonl
          retention-days: 7

      - name: "📊 HERALD Summary"
        run: |
          echo "### 🦅 HERALD Execution Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Events Generated:** $(wc -l < data/events/herald.jsonl || echo 0)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Latest Event:**" >> $GITHUB_STEP_SUMMARY
          tail -n 1 data/events/herald.jsonl | python3 -m json.tool >> $GITHUB_STEP_SUMMARY || echo "No events"

  archivist-audit:
    name: "🔍 ARCHIVIST - Event Verification"
    runs-on: ubuntu-latest
    needs: herald-generate

    steps:
      - name: "📥 Checkout Repository"
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: "🐍 Setup Python"
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: "📦 Install Dependencies"
        run: |
          pip install --upgrade pip
          pip install -r herald/requirements.txt

      - name: "📥 Download HERALD Events"
        uses: actions/download-artifact@v3
        with:
          name: herald-events
          path: .

      - name: "🔍 ARCHIVIST - Audit HERALD Events"
        run: |
          echo "🔍 Running ARCHIVIST audit on HERALD's events..."
          python archivist/shim.py --action audit --agent herald

      - name: "📤 Upload Audit Trail"
        uses: actions/upload-artifact@v3
        with:
          name: audit-trail
          path: data/ledger/audit_trail.jsonl
          retention-days: 30

      - name: "📊 ARCHIVIST Summary"
        run: |
          echo "### 🔍 ARCHIVIST Audit Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Events Audited:** $(wc -l < data/events/herald.jsonl || echo 0)" >> $GITHUB_STEP_SUMMARY
          echo "**Attestations Created:** $(wc -l < data/ledger/audit_trail.jsonl || echo 0)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Latest Attestation:**" >> $GITHUB_STEP_SUMMARY
          tail -n 1 data/ledger/audit_trail.jsonl | python3 -m json.tool >> $GITHUB_STEP_SUMMARY || echo "No attestations"

  commit-ledger:
    name: "📝 Commit Audit Trail to Repository"
    runs-on: ubuntu-latest
    needs: archivist-audit
    if: success()

    permissions:
      contents: write

    steps:
      - name: "📥 Checkout Repository"
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: "📥 Download Audit Trail"
        uses: actions/download-artifact@v3
        with:
          name: audit-trail
          path: data/ledger/

      - name: "📝 Commit Audit Trail"
        run: |
          git config user.name "ARCHIVIST"
          git config user.email "archivist@steward-protocol.local"

          # Ensure directory exists
          mkdir -p data/ledger

          # Add audit trail
          git add data/ledger/audit_trail.jsonl

          if git diff --cached --quiet; then
            echo "ℹ️  No changes to audit trail"
          else
            git commit -m "chore: ARCHIVIST - Audit trail updated by multi-agent federation

This audit trail contains attestations from ARCHIVIST verifying HERALD's events.

Agent: agent.vibe.archivist
Target: agent.steward.herald
Workflow: Multi-Agent Federation
"
            git push origin ${{ github.ref }}
            echo "✅ Audit trail committed and pushed"
          fi

      - name: "✅ Federation Complete"
        run: |
          echo "### 🔗 Multi-Agent Federation Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Agent #1 (HERALD):** Content generation ✅" >> $GITHUB_STEP_SUMMARY
          echo "**Agent #2 (ARCHIVIST):** Event verification ✅" >> $GITHUB_STEP_SUMMARY
          echo "**Audit Trail:** Committed to repository ✅" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "*This demonstrates the STEWARD Protocol's multi-agent coordination capabilities.*" >> $GITHUB_STEP_SUMMARY
