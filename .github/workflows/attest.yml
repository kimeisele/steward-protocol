# DISABLED: steward.cli module does not exist yet
# Re-enable when vibe_core/steward/cli.py is implemented

name: Protocol Attestation (Disabled)

on:
  workflow_dispatch:  # Manual trigger only - disabled for now

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false  # Never run
    steps:
      - run: echo "This workflow is disabled"
