# OPUS-062: PRANA (The Life Force)

> **Sanskrit:** Prana = Life force, Breath, Vital energy
> **Commit:** `57116d7`

## Purpose

Config-Driven Kernel Lifecycle. PRANA enables kernel auto-management without touching VISNU-protected workflow files.

## Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    PRANA (The Life Force)                    │
├─────────────────────────────────────────────────────────────┤
│  .github/workflows/heartbeat.yml  = PROTECTED (immutable)   │
│  config/prana.yaml                = NOT protected (config)  │
│  scripts/heartbeat.py             = reads config, adapts    │
└─────────────────────────────────────────────────────────────┘
```

## Key Features

- **SessionStart Auto-Boot** - Kernel starts in Claude Code Web
- **Config-Driven Behavior** - Change behavior without VISNU bypass
- **Hourly Heartbeat** - `min_interval_minutes: 60`

## Configuration

```yaml
# config/prana.yaml
session_start:
  auto_boot_kernel: true
  background_boot: true

heartbeat:
  min_interval_minutes: 60  # Effective hourly
  boot_kernel_first: true
```

## Key Insight

**"The workflow is immutable. The config is not."**

Workflow says "run every 15 min". Config says "skip if <60 min since last". Result: hourly execution.

---
*Auto-generated by SMRTI (OPUS-064) from git history*
