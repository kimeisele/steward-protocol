# OPUS-060: SATYA (Truth Enforcement)

> **Sanskrit:** Satya = Truth
> **Commit:** `7012f34`

## Purpose

No Silent Test Failures. SATYA enforces truth in the test suite - unknown markers and silent failures become errors.

## Implementation

```toml
# pyproject.toml
[tool.pytest.ini_options]
addopts = "--strict-markers"
filterwarnings = [
    "error::pytest.PytestUnknownMarkWarning",  # SATYA
]
```

## The Problem Solved

Before SATYA:
- 65 tests showed "failed" but never ran
- `pytest-asyncio` was missing
- Tests silently skipped without error

After SATYA:
- Unknown markers = ERROR (not warning)
- Missing dependencies = visible failure
- 1450 tests pass honestly

## Key Insight

> "Das ist nicht 'eine fehlende Abhängigkeit'. Das ist ein systemisches Versagen der Wahrheit."

The system was lying. SATYA stops the lies.

<!-- @HARNESS
type: principle
concept: truth

# Semantic harness - applies to PATTERNS, not specific files
applies_to:
  - pattern: "**/pyproject.toml"
    aspect: Strict test configuration
  - pattern: "**/conftest.py"
    aspect: pytest fixtures
  - pattern: "tests/**/*.py"
    aspect: Test files

# Principles this concept embodies
principles:
  - name: No Silent Failures
    description: Unknown markers are errors, not warnings
  - name: Honest Reporting
    description: Tests that don't run show as errors
  - name: Explicit Dependencies
    description: Missing dependencies fail visibly

# Where to verify this principle is upheld
verification:
  - check: pytest.ini_options has --strict-markers
  - check: Unknown markers raise errors
  - check: All tests actually execute
-->

---
*Auto-generated by SMRTI (OPUS-064) from git history*
