# OPUS-061: AMRITA (Immortal Git State)

> **Sanskrit:** Amrita = Immortal, Nectar of immortality
> **Commit:** `57ed658`

## Purpose

Brain Persistence through Git. AMRITA ensures the system's memory survives container deaths.

## The Problem

In ephemeral containers:
- SQLite databases die when container dies
- In-memory state is lost
- The system has "amnesia" between sessions

## The Solution

**Git IS consciousness.**

```bash
# Before AMRITA - .gitignore had:
.opus_state/

# After AMRITA - .opus_state/ is TRACKED:
# The brain must persist. Git is consciousness.
```

## Key Insight

> "we have lobotomized our fucking selves!"

The `.opus_state/` directory was gitignored. Every session, the brain started fresh. AMRITA fixes this by tracking brain state in Git.

## Files Tracked

- `.opus_state/session.json` - Session state
- `.opus_state/manas_intents.json` - MANAS intentions
- `.opus_state/karma_history.jsonl` - Trust/karma log

<!-- @HARNESS
type: principle
concept: immortality

# Semantic harness - applies to PATTERNS, not specific files
applies_to:
  - pattern: ".opus_state/**"
    aspect: Brain state persistence
  - pattern: ".prakriti/**"
    aspect: Prakriti state layer
  - pattern: ".gitignore"
    aspect: Must NOT ignore brain state
  - event: SESSION_END
    aspect: State must be committed

# Principles this concept embodies
principles:
  - name: Git IS Consciousness
    description: Brain state survives container death via git
  - name: No Amnesia
    description: System remembers across sessions
  - name: Tracked State
    description: .opus_state/ is version controlled

# Where to verify this principle is upheld
verification:
  - check: .opus_state/ is tracked in git
  - check: Session end commits pending state
  - check: Container restart preserves memory
-->

---
*Auto-generated by SMRTI (OPUS-064) from git history*
